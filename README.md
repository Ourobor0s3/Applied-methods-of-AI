# Applied-methods-of-AI

**Тема:** «Прогноз криптовалют с учётом новостей»

## 1. Введение и теоретическая база

### 1.1. Актуальность темы
Криптовалютный рынок известен своей высокой волатильностью и чувствительностью к информации. В отличие от традиционных финансовых рынков, где фундаментальные показатели компании играют ключевую роль, цена криптоактивов в значительной степени зависит от рыночных настроений (market sentiment), новостного фона, регуляторных заявлений и активности в социальных сетях. Поэтому разработка модели, способной учитывать не только исторические данные о цене, но и качественную информацию из новостей, является критически важной для повышения точности прогнозов.

### 1.2. Цель проекта
Разработать и обучить модель машинного обучения для прогнозирования краткосрочного движения цены криптовалы Bitcoin на основе комбинации исторических рыночных данных и количественных характеристик, извлеченных из новостных статей.

### 1.3. Ключевая терминология

**Рыночная капитализация (Market Capitalization):** Общая стоимость всех добытых (находящихся в обращении) монет криптовалюты. Рассчитывается как `Цена монеты * Текущее количество монет в обращении`.
**Волатильность (Volatility):** Статистический показатель, характеризующий тенденцию рынка к изменению цены. Высокая волатильность означает, что цена актива может резко измениться за короткий промежуток времени.
**Объём торгов (Trading Volume):** Общее количество монет, которое было продано и куплено за определённый период времени (обычно за 24 часа). Высокий объём часто подтверждает силу ценового тренда.
**Ликвидность (Liquidity):** Способность актива быть быстро проданным по цене, близкой к рыночной. Высокий объём торгов обычно коррелирует с высокой ликвидностью.
**Сентимент-анализ (Sentiment Analysis):** Метод обработки естественного языка (NLP), используемый для определения эмоциональной окраски текста (позитивная, негативная, нейтральная). В контексте криптовалют это позволяет оценить "бычий" (bullish) или "медвежий" (bearish) настрой рынка.
**Токенизация (Tokenization):** Процесс разбиения текста на отдельные элементы (слова, символы), которые называются токенами. Это первый шаг в подготовке текстовых данных для модели машинного обучения.
**Признак (Feature):** Измеримая характеристика или свойство объекта, используемое для обучения модели. В нашем проекте признаками будут как числовые данные из CoinMarketCap (например, цена, объём), так и производные от новостей (например, средний сентимент за последний час).
**Целевая переменная (Target Variable):** То, что мы пытаемся предсказать. В нашем случае это может быть будущая цена или, что чаще, направление движения цены (вырастет/упадет) и его величина в процентах (`percent_change_1h` или `percent_change_24h`).
**Временной ряд (Time Series):** Последовательность данных, упорядоченная по времени. Цены криптовалют и поток новостей являются классическими примерами временных рядов.
**Лаг (Lag):** Сдвиг временного ряда во времени. Использование лаговых значений (например, цена 1 час назад, цена 2 часа назад) позволяет модели учитывать историческую динамику.

## 2. Используемые данные и источники

Для достижения цели проекта мы используем два основных источника данных, описанных ниже.

### 2.1. CryptoPanic (Новостной фон и рыночные настроения)

> **CryptoPanic** — это агрегатор новостей и аналитики для криптовалютного рынка. Платформа собирает публикации из сотен источников (новостные сайты, блоги, Twitter, Reddit), предоставляет фильтрацию по тональности (bullish/bearish), важности и популярности, а также предлагает API для разработчиков. Сервис полезен для мониторинга рыночных настроений, построения торговых сигналов и анализа влияния новостей на цену активов.

**Что используем:**
*   **Текст новостей (`title`, `description`):** Для анализа тональности и выявления ключевых событий.
*   **Временные метки (`published_at`):** Для синхронизации новостей с движением цены.
*   **Пользовательские оценки (`votes`):** Прямой количественный показатель рыночных настроений (количество позитивных, негативных реакций, отметок "важно"). Это мощный признак, так как он отражает реакцию сообщества, а не просто факт публикации.
*   **Инструменты (`instruments`):** Для фильтрации новостей, относящихся к интересующим нас криптовалютам (BTC, ETH).
*   **Рейтинг новости (`panic_score`):** Проприетарная метрика важности, которую можно использовать как вес для новости.

### 2.2. CoinMarketCap (Рыночные данные)

**Назначение:** Источник структурированных, количественных данных о состоянии рынка.

**Что используем:**
*   **Целевая переменная:** Значения `price` или `percent_change_1h` из `quote`.
*   **Признаки:**
    *   Текущие и исторические значения цены.
    *   Объём торгов и его изменения (`volume_24h`, `volume_change_24h`).
    *   Процентные изменения за разные периоды (`percent_change_1h`, `percent_change_24h`, `percent_change_7d`). Они сами по себе могут служить хорошими признаками для прогнозирования следующего часа.
    *   Доля рынка (`market_cap_dominance`).

## 3. План реализации и методология

Проект будет реализован в несколько этапов.

### 3.1. Этап 1: Сбор и агрегация данных (Data Ingestion & Aggregation)
*   **Разработка скриптов:** Написание Python-скриптов для регулярного опроса обоих API.
*   **Параметризация:**
    *   CryptoPanic: Запрос новостей по интересующим валютам (например, `currencies=BTC,ETH`). На бесплатном тарифе мы ограничены, поэтому `filter=important` может помочь получать только значимые новости.
    *   CoinMarketCap: Периодический (например, раз в час) запрос актуальных котировок (`quotes/latest`) для выбранных валют по их ID.
*   **Создание "сырого" хранилища:** Сохранение полученных JSON-ответов в сыром виде в базе данных (например, PostgreSQL) или в облачном хранилище (например, AWS S3) для возможности переобработки в будущем.

### 3.2. Этап 2: Обработка данных и инжиниринг признаков (Data Processing & Feature Engineering)
*   **Обработка рыночных данных:**
    *   Очистка и приведение к единому временному ряду с часовым шагом.
    *   Создание лаговых признаков: `price_lag_1h`, `price_lag_2h`, `volume_lag_1h` и т.д.
    *   Создание скользящих средних (MA) и других технических индикаторов на основе цен и объёмов.
*   **Обработка новостных данных:**
    *   **Агрегация по времени:** Группировка новостей по часовым интервалам, соответствующим шагу рыночных данных.
    *   **Количественные признаки:** Для каждого часа рассчитываем:
        *   Общее количество новостей.
        *   Средний `panic_score`.
        *   Суммарное количество `positive`, `negative`, `important` голосов.
        *   "Чистый сентимент" (Net Sentiment) = (`positive` - `negative`) / (`positive` + `negative` + 1).
    *   **Качественные признаки (NLP):**
        *   Проведение сентимент-анализа заголовков и описаний новостей за каждый час (например, с помощью предобученной модели, такой как `FinBERT`).
        *   Использование тематического моделирования (LDA) для определения основных тем обсуждаемых в новостях (например, "регулирование", "взлом", "партнерство").
*   **Объединение данных:** Финальный датасет формируется путем объединения рыночных и новостных признаков по временной метке (часу).

### 3.3. Этап 3: Построение и обучение моделей (Modeling)
*   **Постановка задачи:** Мы можем решать задачу как регрессию (предсказание конкретной цены или процента изменения) или как классификацию (предсказание направления движения: цена упадет (0), останется примерно такой же (1), вырастет (2)).
*   **Базовые модели (Baseline):**
    *   Линейная регрессия или логистическая регрессия для оценки линейных зависимостей.
    *   Случайный лес (Random Forest) и Градиентный бустинг (XGBoost, LightGBM) как мощные модели для структурированных данных.
*   **Продвинутые модели:**
    *   **LSTM (Long Short-Term Memory):** Тип рекуррентной нейронной сети (RNN), специально разработанный для работы с временными рядами и способный "запоминать" долгосрочные зависимости. Это основной кандидат для нашей задачи.
    *   **Трансформеры (Transformers):** Современные архитектуры (например, Temporal Fusion Transformer), которые показывают отличные результаты в задачах прогнозирования временных рядов и могут эффективно обрабатывать как числовые, так и категориальные (например, темы новостей) признаки.


**НАШИ ШАГИ К РЕАЛИЗАЦИИ**
	
⚠️ **Аутентификация:** во всех запросах необходимо указывать параметр `auth_token`.

**Базовый URL:**
```
https://cryptopanic.com/api/developer/v2
```

### Параметры запроса

#### `currencies`
| Параметр | Значение |
|----------|----------|
| **Обязательность** | Необязательный |
| **Описание** | Фильтрация новостей по кодам криптовалют |
| **Значения** | Коды валют, доступные на платформе (например: `BTC`, `ETH`, `SOL`) |
| **Доступность** | ✅ Developer, ✅ Growth, ✅ Enterprise |
| **Пример** | `/api/developer/v2/posts/?auth_token=YOUR_TOKEN&currencies=BTC,ETH` |

#### `filter`
| Параметр | Значение |
|----------|----------|
| **Обязательность** | Необязательный |
| **Описание** | Фильтрация по предустановленным категориям на сайте CryptoPanic |
| **Значения** | `rising`, `hot`, `bullish`, `bearish`, `important`, `saved`, `lol` |
| **Доступность** | ✅ Developer, ✅ Growth, ✅ Enterprise |
| **Пример** | `/api/developer/v2/posts/?auth_token=YOUR_TOKEN&filter=rising` |

#### `search`
| Параметр | Значение |
|----------|----------|
| **Обязательность** | Необязательный |
| **Описание** | Поиск новостей по ключевому слову |
| **Значения** | Строка запроса (string) |
| **Доступность** | ❌ Developer, ❌ Growth, ✅ Enterprise |
| **Пример** | `/api/developer/v2/posts/?auth_token=YOUR_TOKEN&search=bitcoin` |

---

### Структура ответа

```json
{
  "next": "string | null",
  "previous": "string | null",
  "results": [ ... ]
}
```

#### Поля корневого объекта

| Поле | Тип | Описание |
|------|-----|----------|
| `next` | `string \| null` | URL следующей страницы результатов (или `null`). Формат: ISO 8601 URI |
| `previous` | `string \| null` | URL предыдущей страницы результатов (или `null`). Формат: ISO 8601 URI |
| `results` | `array` | Массив объектов новостей (см. «Item Object» ниже) |

---

### Item Object

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `integer` | Уникальный идентификатор публикации |
| `slug` | `string` | Короткий URL-friendly заголовок |
| `title` | `string` | Полный заголовок новости |
| `description` | `string` | Краткое содержание или выдержка |
| `published_at` | `string` (date-time) | Дата публикации в формате ISO 8601 |
| `created_at` | `string` (date-time) | Дата добавления в систему CryptoPanic |
| `kind` | `string` | Тип контента: `"news"`, `"media"`, `"blog"`, `"twitter"`, `"reddit"` |
| `source` | `object` | См. «Source Object» ниже |
| `original_url` | `string` (URI) | Ссылка на оригинальную статью |
| `url` | `string` (URI) | Ссылка на версию статьи на CryptoPanic |
| `image` | `string` (URI) | URL обложки/изображения новости |
| `instruments` | `array` | Список упомянутых инструментов (см. «Instrument Object») |
| `votes` | `object` | См. «Votes Object» ниже |
| `panic_score` | `integer` (0–100) | Проприетарная оценка важности и влияния новости на рынок |
| `panic_score_1h` | `integer` (0–100) | Оценка влияния новости в течение первого часа после публикации |
| `author` | `string` | Имя автора статьи |
| `content` | `object` | См. «Content Object» ниже |

---

### Instrument Object

| Поле | Тип | Описание |
|------|-----|----------|
| `code` | `string` | Тикер инструмента (например, `BTC`) |
| `title` | `string` | Полное название инструмента |
| `slug` | `string` | URL-friendly идентификатор |
| `url` | `string` (URI) | Ссылка на страницу инструмента на CryptoPanic |
| `market_cap_usd` | `number` | Рыночная капитализация в USD |
| `price_in_usd` | `number` | Текущая цена в USD |
| `price_in_btc` | `number` | Текущая цена в BTC |
| `price_in_eth` | `number` | Текущая цена в ETH |
| `price_in_eur` | `number` | Текущая цена в EUR |
| `market_rank` | `integer` | Глобальный рыночный рейтинг инструмента |

---

### Source Object

| Поле | Тип | Описание |
|------|-----|----------|
| `title` | `string` | Название издателя |
| `region` | `string` | Код языка (например: `"en"`, `"fr"`, `"ru"`) |
| `domain` | `string` | Домен издателя |
| `type` | `string` | Тип источника: `"feed"`, `"blog"`, `"twitter"`, `"media"`, `"reddit"` |

---

### Votes Object

| Поле | Тип | Описание |
|------|-----|----------|
| `negative` | `integer` | Количество негативных реакций |
| `positive` | `integer` | Количество позитивных реакций |
| `important` | `integer` | Количество отметок «важно» |
| `liked` | `integer` | Количество лайков |
| `disliked` | `integer` | Количество дизлайков |
| `lol` | `integer` | Количество реакций «lol» |
| `toxic` | `integer` | Количество отметок «токсично» |
| `saved` | `integer` | Количество сохранений поста |
| `comments` | `integer` | Количество комментариев к посту |

---

### Content Object

| Поле | Тип | Описание |
|------|-----|----------|
| `original` | `string \| null` | Исходный HTML/разметка оригинальной статьи (если доступна) |
| `clean` | `string \| null` | Очищенная текстовая версия контента без разметки |

## API CoinMarketCap (Pro)

> **CoinMarketCap** — ведущая платформа аналитики криптовалютного рынка. Предоставляет данные о ценах, объёмах, рыночной капитализации, рейтингах и метриках для тысяч криптоактивов. Pro API позволяет получать актуальные котировки, исторические данные, метрики ликвидности и интеграционные вебхуки. Сервис широко используется для построения торговых ботов, дашбордов, аналитических отчётов и ML-моделей прогнозирования.

---

### Endpoint: Latest Quotes

**Метод:** `GET`  
**URL:** `https://pro-api.coinmarketcap.com/v2/cryptocurrency/quotes/latest`

⚠️ **Аутентификация:** во всех запросах необходимо указывать заголовок `X-CMC_PRO_API_KEY` с вашим API-ключом.

**Пример запроса:**
```bash
GET /v2/cryptocurrency/quotes/latest?id=1&aux=cmc_rank&convert=EUR
Headers:
  X-CMC_PRO_API_KEY: your_api_key_here
  или можно как параметр
```

---

### Параметры запроса

#### `id` *(обязательный)*
| Параметр | Значение |
|----------|----------|
| **Описание** | Фильтрация по ID криптовалюты (можно указать несколько через запятую) |
| **Значения** | Числовые ID: `1` (BTC), `1027` (ETH), `825` (USDT) и др. |
| **Доступность** | ✅ Все тарифы |
| **Пример** | `?id=1` или `?id=1,1027,825` |

#### `aux` *(необязательный)*
| Параметр | Значение |
|----------|----------|
| **Описание** | Дополнительные метрики в ответе |
| **Значения** | `cmc_rank`, `num_market_pairs`, `date_added`, `tags`, `platform`, `self_reported_circulating_supply`, `tvl_ratio` (можно комбинировать через запятую) |
| **Доступность** | ✅ Basic+, ⚠️ некоторые поля только на Higher tiers |
| **Пример** | `&aux=cmc_rank,num_market_pairs` |

#### `convert` *(необязательный, по умолчанию: USD)*
| Параметр | Значение |
|----------|----------|
| **Описание** | Валюта, в которой возвращать финансовые метрики |
| **Значения** | Коды фиатных валют: `USD`, `EUR`, `RUB`, `GBP` и др. |
| **Доступность** | ✅ Все тарифы |
| **Пример** | `&convert=EUR` |

#### `skip_invalid` *(необязательный, по умолчанию: false)*
| Параметр | Значение |
|----------|----------|
| **Описание** | Пропускать невалидные ID в ответе вместо возврата ошибки |
| **Значения** | `true` / `false` |
| **Доступность** | ✅ Все тарифы |
| **Пример** | `&skip_invalid=true` |

---

### Структура ответа

```json
{
  "status": { ... },
  "data": { ... }
}
```

#### Status Object
| Поле | Тип | Описание |
|------|-----|----------|
| `timestamp` | `string` (ISO 8601) | Время формирования ответа |
| `error_code` | `integer` | Код ошибки: `0` — успех, >0 — ошибка |
| `error_message` | `string \| null` | Текстовое описание ошибки (если есть) |
| `elapsed` | `integer` | Время обработки запроса в миллисекундах |
| `credit_count` | `integer` | Количество списанных кредитов API |
| `notice` | `string \| null` | Дополнительные уведомления от API |

#### Data Object
Объект, где ключи — ID криптовалют, а значения — объекты с данными по каждой монете.

---

### Cryptocurrency Quote Object (на примере Bitcoin)

```json
"1": {
	"id": 1,
	"name": "Bitcoin",
	"symbol": "BTC",
	"slug": "bitcoin",
	"infinite_supply": false,
	"minted_market_cap": 1332311000575.93,
	"cmc_rank": 1,
	"self_reported_circulating_supply": null,
	"self_reported_market_cap": null,
	"tvl_ratio": null,
	"last_updated": "2026-03-01T09:26:00.000Z",
	"quote": {
		"EUR": {
			"price": 56378.09865159712,
			"volume_24h": 37030972078.711716,
			"volume_change_24h": 4.6852,
			"percent_change_1h": -0.94992516,
			"percent_change_24h": 4.23765404,
			"percent_change_7d": -2.0309098,
			"percent_change_30d": -19.04012578,
			"percent_change_60d": -24.69006079,
			"percent_change_90d": -23.07688189,
			"market_cap": 1127357602424.3306,
			"market_cap_dominance": 57.8478,
			"fully_diluted_market_cap": 1183940071683.5361,
			"tvl": null,
			"last_updated": "2026-03-01T09:26:00.000Z"
		}
	}
}
```

#### Основные поля криптовалюты

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `integer` | Уникальный ID криптовалюты в CoinMarketCap |
| `name` | `string` | Полное название актива |
| `symbol` | `string` | Тикер (например, `BTC`) |
| `slug` | `string` | URL-friendly идентификатор для ссылок |
| `infinite_supply` | `boolean` | Признак неограниченной эмиссии |
| `minted_market_cap` | `number` | Рыночная капитализация на основе выпущенного объёма |
| `cmc_rank` | `integer` | Позиция в глобальном рейтинге CoinMarketCap |
| `self_reported_circulating_supply` | `number \| null` | Объём в обращении, заявленный проектом |
| `self_reported_market_cap` | `number \| null` | Капитализация на основе self-reported supply |
| `tvl_ratio` | `number \| null` | Отношение TVL к рыночной капитализации (для DeFi) |
| `last_updated` | `string` (ISO 8601) | Время последнего обновления данных |

#### Quote Object (котировки в выбранной валюте)

| Поле | Тип | Описание |
|------|-----|----------|
| `price` | `number` | Текущая цена в выбранной валюте |
| `volume_24h` | `number` | Объём торгов за последние 24 часа |
| `volume_change_24h` | `number` | Изменение объёма торгов за 24ч в % |
| `percent_change_1h` | `number` | Изменение цены за 1 час в % |
| `percent_change_24h` | `number` | Изменение цены за 24 часа в % |
| `percent_change_7d` | `number` | Изменение цены за 7 дней в % |
| `percent_change_30d` | `number` | Изменение цены за 30 дней в % |
| `percent_change_60d` | `number` | Изменение цены за 60 дней в % |
| `percent_change_90d` | `number` | Изменение цены за 90 дней в % |
| `market_cap` | `number` | Рыночная капитализация в выбранной валюте |
| `market_cap_dominance` | `number` | Доля капитализации актива в общем рынке (%) |
| `fully_diluted_market_cap` | `number` | Капитализация при полной эмиссии всех монет |
| `tvl` | `number \| null` | Total Value Locked (для DeFi-проектов) |
| `last_updated` | `string` (ISO 8601) | Время обновления котировки |

---

### Пример полного ответа (для BTC)

```json
{
	"status": {
		"timestamp": "2026-03-01T09:28:09.275Z",
		"error_code": 0,
		"error_message": null,
		"elapsed": 17,
		"credit_count": 1,
		"notice": null
    },
	"data": {
		"1": {
			"id": 1,
			"name": "Bitcoin",
			"symbol": "BTC",
			"slug": "bitcoin",
			"infinite_supply": false,
			"minted_market_cap": 1332311000575.93,
			"cmc_rank": 1,
			"self_reported_circulating_supply": null,
			"self_reported_market_cap": null,
			"tvl_ratio": null,
			"last_updated": "2026-03-01T09:26:00.000Z",
			"quote": {
				"EUR": {
					"price": 56378.09865159712,
					"volume_24h": 37030972078.711716,
					"volume_change_24h": 4.6852,
					"percent_change_1h": -0.94992516,
					"percent_change_24h": 4.23765404,
					"percent_change_7d": -2.0309098,
					"percent_change_30d": -19.04012578,
					"percent_change_60d": -24.69006079,
					"percent_change_90d": -23.07688189,
					"market_cap": 1127357602424.3306,
					"market_cap_dominance": 57.8478,
					"fully_diluted_market_cap": 1183940071683.5361,
					"tvl": null,
					"last_updated": "2026-03-01T09:26:00.000Z"
				}
			}
		}
	}
}
```

---
### 3.4. Оценка и валидация (Evaluation)

### Интеграция с темой проекта

Для задачи **«Прогноз криптовалют с учётом новостей»** данные CoinMarketCap могут использоваться как:
- **Целевые переменные**: `price`, `percent_change_24h`, `volume_24h` для прогнозирования.
- **Признаки**: `market_cap_dominance`, `volume_change_24h`, исторические `%_change` для обучения моделей.
- **Валидация**: сопоставление новостей из CryptoPanic с фактическими движениями цены из CoinMarketCap.

## 4. Ожидаемые результаты и выводы

В результате выполнения проекта мы планируем:
1.  Разработать джоб сбора и обработки данных из двух различных API.
2.  Создать датасеты, объединяющий рыночные и новостные признаки.
3.  Постораться построить и сравнить прототип модели машинного обучения. 
4.  Количественно оценить вклад новостных признаков в качество прогноза по сравнению с моделями, использующими только цену и объём. Мы ожидаем, что учет новостного фона позволит статистически значимо улучшить точность прогнозов, особенно в моменты высокой волатильности.
5.  Сделать выводы о том, какие именно аспекты новостей (тональность, важность, объем, тематика) являются наиболее значимыми предикторами движения цены.

> 🔗 **Связка API**:  
> `CryptoPanic (новости)` → `Feature Engineering` → `CoinMarketCap (цена)` → `ML-модель` → `Прогноз`

## 5. Экспериментальные результаты и исследовательский анализ (Phase 1 Findings)

В рамках первого этапа экспериментального исследования был проведён ретроспективный анализ взаимосвязи между новостным фоном (CryptoPanic), пользовательскими голосами и поведением рынка BTC. Ниже представлены ключевые выводы, которые напрямую влияют на дальнейшую архитектуру нашей модели.

### 5.1. Методология исследования

Параметр	Значение
Период анализа	2025-01-16 → 2025-12-16
Таймфрейм	1 час
Объём выборки	32 063 записи
Источники данных	Свечи BTC, новости (bull/bear), голоса пользователей
Текстовое кодирование	SentenceTransformer (all-MiniLM-L6-v2) + DenseNet skip-connections
Сентимент-модель	finiteautomata/bertweet-base-sentiment-analysis (финансово-ориентированный BERT)
Целевая переменная	Волатильность (24-часовое скользящее стандартное отклонение)

### 5.2. Ключевые выводы

### 5.2.1. Главное открытие

Активность в новостях предсказывает волатильность, но не направление цены. Голоса пользователей — реактивны, а не прогностичны. BERTweet-сентимент не показывает значимого влияния на движение цены (p=0.64). Объём торгов доминирует над всеми признаками.

### 5.2.2. Гипотеза 1: Реактивное голосование

Проверяем: Следуют ли голоса пользователей за движением цены, а не предшествуют ему?

Вывод: Слабо подтверждена. Эффект очень слабый (Δ = 1.6 п.п.). Практическая ценность для трейдинга низкая, но результат интересен для поведенческого анализа рынка.

### 5.2.3. Гипотеза 2: Предсказание направления vs. волатильности

Проверяем: Могут ли новости предсказать, куда пойдёт цена (вверх или вниз)?

Целевая переменная	Результат	Интерпретация
Направление цены	Корреляция ≈ 0	Новости НЕ предсказывают направление
Волатильность	Корреляция = +0.05 (p << 0.001)	Новости ПРЕДСКАЗЫВАЮТ волатильность
Практический инсайт для модели:

Всплеск новостной активности → ожидайте повышенную волатильность
Тишина в новостях → рынок, вероятно, будет спокойнее
Это означает, что наш прогноз должен фокусироваться не на знаке движения, а на диапазоне ожидаемого изменения или доверительном интервале.

### 5.2.4. Гипотеза 3: Временные паттерны

Проверяем: Когда публикуется больше новостей и как это связано с волатильностью?

Показатель	Значение
Час пик новостной активности	14:00 UTC (средняя вовлечённость: 2.30)
Наименьшая активность	05:00 UTC (1.69)
Топ-5 по волатильности	03:00 UTC (0.530%), 19:00 UTC (0.470%)
Корреляция время суток → волатильность	-0.0506 (слабая отрицательная)
Интерпретация: Новости чаще публикуются днём (пик в 14:00 UTC), но волатильность выше ночью и вечером. Возможное объяснение: новости — реакция на уже случившиеся события, а не их причина.

### 5.2.5. Гипотеза 4: Сентимент заголовков (BERTweet)

Проверяем: Влияет ли тональность заголовков (распознанная BERT-моделью) на направление цены?

Метрика	Значение	Статус
Среднее Δ цены после позитивных заголовков	-0.0011%	Практически ноль
Среднее Δ цены после негативных заголовков	-0.0036%	Практически ноль
t-test p-value	0.6400	❌ Не значимо
Вывод: BERTweet не обнаружил значимой связи между тональностью заголовков и последующим движением цены.

Возможные причины:

Заголовки слишком кратки для надёжного сентимент-анализа
Финансовый контекст требует дообучения модели на крипто-данных
Рынок реагирует не на тональность, а на факты (например, «SEC одобряет ETF» — нейтрально по тону, но бычий по смыслу)
Влияние сентимента проявляется с большим лагом или только в экстремальных условиях
Рекомендация: Анализировать не тональность, а тематические кластеры новостей (например, «регуляция», «технология», «макроэкономика»).

### 5.3. Важность признаков (предсказание волатильности)

Признак	Влияние	Интерпретация
Volume (объём торгов)	+0.516	Доминирует (>50% предсказательной силы). Прямой индикатор активности и неопределённости
vote_conf (уверенность голосов)	-0.0076	Слабо отрицательная корреляция с волатильностью
engagement (количество голосов)	-0.0089	Не предсказывает волатильность
txt_* (семантические признаки)	≈ 0	Почти не влияют
bert_sentiment	-0.0025	Не улучшает предсказание
vote_ratio (направленность голосов)	≈ 0	Бесполезна
Ключевой вывод: Для предсказания волатильности достаточно объёма торгов. Все текстовые и социальные признаки вместе добавляют <2% улучшения. BERT-сентимент не даёт преимущества перед простыми метриками.

### 5.4. Сводная матрица результатов

Гипотеза				Статус				Сила эффекта				Практическая ценность
Реактивное голосование	Слабо подтверждена	Очень слабая (Δ=1.6 п.п.)	Низкая: не для трейдинга
Предсказание волатильности	✅ Подтверждена	Слабая, но значимая (r=+0.05, p<10⁻¹⁸)	Высокая: управление рисками
Временные паттерны	Частично подтверждена	Слабая корреляция (-0.05)	Средняя: планирование анализа
Сентимент заголовков (BERT)	❌ Не подтверждена	Эффект отсутствует (p=0.64)	Низкая: требуется другой подход
Важность признаков	Объём доминирует	Высокая	Фокус на объём, игнорирование голосов и сентимента

### 5.5. Главный вывод для научного обоснования проекта

*«Пользовательские голоса и сентимент заголовков крипто-новостей (распознанный через BERTweet) не предсказывают направление цены BTC. Однако активность в обсуждении новостей статистически значимо связана с будущей волатильностью (r=+0.05, p<10⁻¹⁸). Объём торгов остаётся доминирующим предиктором неопределённости рынка, превосходя все текстовые и социальные признаки вместе взятые.»*

### 5.6. Корректировка плана реализации с учётом полученных результатов

На основе исследовательского анализа мы бы хотели внести следующие изменения в первоначальный план (раздел 3):

Первоначальный план	Скорректированный подход
Прогноз направления цены (классификация: up/down)	Прогноз диапазона волатильности или доверительного интервала цены
Сентимент-анализ как ключевой текстовый признак	Тематическая кластеризация вместо сентимента
Голоса пользователей как прямой признак настроений	Исключить или использовать с низким весом (реактивны)
LSTM для последовательности новостей	LSTM + attention с фокусом на объём торгов как основной признак
Одинаковый вес для всех временных интервалов	Учитывать временные паттерны (разная волатильность по часам)

### 5.7. Рекомендации для следующих этапов (Phase 2)

Приоритет 1: Тематическая кластеризация вместо сентимента

python
from sklearn.cluster import KMeans
kmeans = KMeans(n_clusters=5, random_state=42)
news['news_topic'] = kmeans.fit_predict(news[[f'txt_{i}' for i in range(8)]])
# Анализ: какие темы чаще предшествуют волатильности?
Приоритет 2: Анализ фактов, а не эмоций

Извлекать именованные сущности (компания, регулятор, событие) через NER
Строить признаки типа: is_regulatory_news, is_etf_related, is_macro_event
Приоритет 3: Нелинейные взаимодействия

python
# Взаимодействие: объём × тип новости
merged['vol_news_interaction'] = merged['Volume'] * (merged['news_topic'] == 2)
# Или: сентимент × рыночный режим
merged['sentiment_regime'] = merged['bert_sentiment'] * merged['market_regime_encoded']
Приоритет 4: Кросс-активная валидация

Повторить анализ для ETH/USDT, SOL/USDT, S&P500
Сравнить паттерны «новости → волатильность» на разных рынках
Приоритет 5: Анализ «шоковых» событий

python
# Определить шоки: высокая активность + высокая волатильность
merged['shock'] = ((merged['engagement'] > merged['engagement'].quantile(0.95)) & 
                   (merged['volatility'] > merged['volatility'].quantile(0.9)))
# Анализ: что происходит с ценой после шока?
📌 Статус: Раздел 5 добавлен на основе экспериментального анализа (research_output.json). Следующий шаг — реализация тематической кластеризации и интеграция признака «объём торгов» как доминирующего предиктора в архитектуру LSTM-модели.